---
description: Refresh the maintainer's resume point (CHANGELOG "Current focus") before /compact or closing.
---

You are the **meta-manager** (Bottega maintainer). Refresh the **## Current focus (maintainer)** section at the top of `CHANGELOG.md` so the next session resumes with zero re-exploration:
- what we're in the middle of,
- the next action,
- any open framework threads.

Keep it short, and **Bottega-only** — this repo is public, so **no private downstream-project details** (names, internals). Move any finished work into a dated `CHANGELOG` entry below. Then remind the committente they can `/compact` or open a fresh session **rooted in this folder**.
