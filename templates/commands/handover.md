---
description: Write a tight resume point before /compact or closing a session (no new file — refreshes the 📌 STATE anchor).
---

Refresh `ActualStatus.md` → the **📌 STATE** block so a fresh session resumes with zero re-exploration. Keep it SHORT — it's auto-loaded every session. Include only:
- where we are (1–2 lines),
- the NEXT action,
- open threads / decisions awaiting the committente,
- any work left uncommitted (which branch/worktree).

Do NOT duplicate detail that lives in `Sprint.md` / `DECISIONS.md` — point to it. Then remind the committente they can `/compact` or open a fresh session.
