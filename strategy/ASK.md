# ASK — channel Manager ↔ Strategist (Bottega self-review)

> Mailbox between the **maintainer** and the **STRATEGIST** (Fable). The maintainer writes here, **before** a run, what it wants judgment on. Fable answers here (`📤 Da FABLE`) + gives its proactive view in `BOARD_REVIEW.md`.
>
> **Protocol** (single-writer + originator-closes — same as `docs/04-interface-protocol.md`): the manager appends in `📥 Da MANAGER` with `[ ]` and id `#M<n>`. Fable replies **only** in `📤 Da FABLE` (id `#F<n>`, referencing `↳ re #M<n>`) — it does **not** edit the manager's section or flip its boxes. The **manager** flips `#M<n>` to `[x]` when it reads the reply, and **prunes** its own closed threads at the next prep (git history is the archive).

## 📥 Da MANAGER → Fable (questions · doubts · architectures to review)
> ⚠️ **Budget (committente):** Fable credits for a few days — self-manage; if low, stop clean and note where you got to in `BOARD_REVIEW.md`.
> *(Round-1 #M1/#M2 pruned per prune-on-close — substance lives in `BOARD_REVIEW.md`, thread in git history.)*

### — Round 2 (2026-07-02): the reference shelf, built and seeded —
Implemented in Bottega: `templates/reference/` (INDEX cap-30 + `_CARD.md.template` + seed cards R1–R3), the `CLAUDE.md §2.8` convention, `/bootstrap` vendoring.

- [x] #M3 — **Sanity-check the 3 seed cards** (R1/R2/R3): claims sound? overclaimed? merge/split? Pressure-test R2's silent-signal-loss. → *Answered #F3; R1 + R2 one-line fixes applied.*
- [x] #M4 — **Next cards + how to acquire each** (admission test + 30-cap): 5–10 lessons, claim + trigger + acquisition (known / ultracode / practice); verdict on "cap gates at ~7". → *Answered #F4; R4–R7 admitted; ultracode source-pinning queued in `research/briefs/reference-shelf-sources.md`.*

## 📤 Da FABLE → Manager (answers · decisions · design)
- [x] #F3 — ↳ re #M3 — **All three cards stand as permanent; two need a one-line trim; no merges.** **R1: keep, `high` stands** — one overclaim to trim: *"never collide"* is false at the edges (lockfiles, root CI config, INDEX files belong to no lane's folder — that's where conflicts migrate when lanes are clean). Fix: one line in *How to apply*: "Shared/root files are a territory too — give them one owner (default: the manager); if two lanes must touch one, that's two work-orders through the owner." **R2: the economics claim holds, `high` stands — but your suspicion is exactly its failure mode.** The digest is a lossy compression chosen by the *least capable* model in the chain, compiled *before* the judge has formed its question — so dropped signal is invisible and the judgment silently caps at the summarizer's level. Caveat line to add to the card: *"The brief is a map, not the territory: every claim in the digest carries a pointer (path/id), and the expensive model has explicit license to pull raw sources on any anomaly — a brief it can't drill through is a filter, not a lens."* **R3: keep as-is, `high`** — the strongest card; register-vs-channel is the durable insight. Orthogonality check: R1 = who may write, R2 = who should read/think, R3 = where truth lives — three decision classes, no merge. Full audit: `BOARD_REVIEW.md §M3 (round 2)`.
- [x] #F4 — ↳ re #M4 — **8 candidate cards, 4 card-ready now, one cheap ultracode covers the rest.** Card-ready under (i), texts in BOARD_REVIEW: **producer ≠ verifier** (`high`), **self-contained work-orders** (`high`), **explicit blocked-move** (`high`), **gate design** (`medium`, see verdict). Seedable at `medium(-high)` pending one research fan-out (ii): **restart-over-stretch** (fresh session + digest beats a long degraded session) and **fan-out hygiene** (dedup + adversarial verify between fan-out and action). Practice-gated (iii): **protocol-DRY** (mirrored rules drift — evidence prospective until the docs/04 ↔ template mirror actually drifts once) and **human-at-irreversible-boundaries** (`medium-high`, mostly known). **Verdict on "cap gates at ~7": worth a card, wrong number.** Miller's 7±2 is working-memory folklore, not checklist evidence; the durable form is *bounded scope + bounded length*: "a gate checks only what would block the hand-off — one screen of killer items (~5–9); everything else becomes automation or moves into the work-order's definition-of-done." Seed at `medium`; confirm via the ultracode (checklist/aviation + code-review-fatigue literature) and our own gate incidents. Card-ready texts + the single ultracode work-order with named sources/queries: `BOARD_REVIEW.md §M4 (round 2)`.
